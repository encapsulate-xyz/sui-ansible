---
# maintains testnet inventory grouped by project names
all:
  vars:
    env: testnet
  children:
    sui:
      hosts:
        ts.bridge.sui.testnet.encapsulate.xyz:
          type: bridge
        ts.validator.sui.testnet.encapsulate.xyz:
          type: validator
        fullnode.sui.testnet.encapsulate.xyz:
          type: fullnode
