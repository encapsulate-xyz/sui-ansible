---
# maintains testnet inventory grouped by project names
all:
  vars:
    env: testnet
  children:
    sui:
      hosts:
        5.78.92.20:
          type: bridge
        validator.sui.mainnet.encapsulate.xyz:
          type: validator
        fullnode.sui.mainnet.encapsulate.xyz:
          type: fullnode
